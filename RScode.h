#ifndef __RScode_h__
#define __RScode_h__

#include "globalFile.h"
#include "myIO.h"
#include "simulate.h"

bool RS_valid(const VB &oligo);//check whether an oligo is a RS codeword

int RS_info_length();//number of information bits for one oligo

VB RS_encode(const VB &info);//RS encoder: info + parity-check bits (return oligo)
VB RS_decode(const VB &noiseCode);//RS decoder; return empty VB for failure
VB RS_extract_info(const VB &code);//extract infomation from oligo (RS codeword)

//read [read_size] many oligos uniformly at random from [write_oligo].
//each row is an extended encoded symbol of form (seed, xor_info, num).
//[seed] is the first integer identifying source symbols generating [xor_info],
//[xor_info] is a VI generated by xoring source symbols, and
//[num] is the last integer indicating the number of copies.
//negative (non-negative) num indicates valid but incorrect (correct) output
VVI RS_read(const VVI &write_oligo);


#endif
